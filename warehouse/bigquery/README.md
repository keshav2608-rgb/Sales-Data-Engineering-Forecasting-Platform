# BigQuery Warehouse

See `docs/warehouse.md` for full setup. This folder contains:
- `schema.sql` - table DDL (partitioned/clustered) for sales_staging and sales_forecast
- `setup.sql` - dataset creation reference

Not executed in this build sandbox (no GCP credentials available) - written
and ready to run against a real project.
